#config_version=6

config:
- hardware.yaml
- displays.yaml
- devices.yaml
- infolights.yaml
- switches.yaml
- coils.yaml
- lights.yaml

mpf:
  report_crashes: never

machine:    
  balls_installed: 3
  min_balls: 3

playfields:
  playfield:
    default_source_device: bd_plunger_lane
    tags: default

game:
  balls_per_game: 2

modes:
  - base